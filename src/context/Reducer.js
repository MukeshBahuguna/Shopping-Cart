import React from 'react'

export const cartReducer = (state ,action) => {
  
    switch (action.type) {
        case 'value':
            
            return;
    
        default:
            return state;
    }

}
